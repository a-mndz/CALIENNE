pydantic>=2.0.0
pydantic-settings>=2.0.0
json-repair>=0.12.0
httpx>=0.24.0
python-dotenv>=1.0.0
fastapi>=0.110.0
# CVE-2025-54121 (starlette <0.47.2, multipart DoS). Transitive via fastapi;
# floor pinned explicitly so no resolver can walk below it. The frozen
# resolution lives in requirements.lock (uv pip compile, hashed).
starlette>=0.47.2
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.0
asyncpg>=0.29.0
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
tiktoken>=0.5.0
keyring>=25.0.0
alembic>=1.13.0
aiosqlite>=0.20.0
# Experience DB (RFC-004 §7 / ADR-008). Testcontainers spins a disposable
# PostgreSQL for the round-trip integration tests (§7.4). pgvector is installed
# but UNUSED in v1 — it lights up in v2 against experience_learning (DEC-007).
testcontainers[postgres]>=4.0.0
pgvector>=0.2.0

# Observability. structlog renders JSON logs in production and a human-readable
# console stream in dev (core/config.py). prometheus-client backs the /metrics
# endpoint (orchestrator/metrics.py). OpenTelemetry is installed in the local
# venv but intentionally undeclared until the tracing step lands.
structlog>=26.1.0
prometheus-client>=0.24.1
backports.tarfile>=1.2.0; python_version < '3.12'

